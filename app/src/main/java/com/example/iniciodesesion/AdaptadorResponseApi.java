package com.example.iniciodesesion;

public class AdaptadorResponseApi /*extends RecyclerView.Adapter<ListAdapter.ViewHolder>*/ {
   /* private List<ListElement> miData;
    private LayoutInflater miFlater;
    private Context context;

    public AdaptadorResponseApi(List<ListElement> list, Context contet)
    {
        this.miFlater = LayoutInflater.from(contet);
        this.context = contet;
        this.miData = list;
    }

    @Override
    public int getItemCount() { return miData.size(); }

    @Override
    public AdaptadorResponseApi.ViewHolder onCreateViewHolder (ViewGroup parent, int viewType) {
        View view = miFlater.inflate(R.layout.activity_bienvenido, null);
        return new AdaptadorResponseApi.ViewHolder(view);
    }

    @Override
    public void onBindViewHolder(final AdaptadorResponseApi.ViewHolder holder, final int position)
    {
        holder.bindData(miData.get(position));
    }

    public class ViewHolder extends RecyclerView.ViewHolder{
        RecyclerView rcvAdapter;

        ViewHolder(View itemView)
        {
            rcvAdapter = itemView.findViewById(R.id.rcvDatosApi);
        }

        void bindData(final ListElement item)
        {
            rcvAdapter.setAdapter());
        }
    }*/

}
